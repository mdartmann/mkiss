#!/bin/sh -uxe
export DESTDIR="$1"

meson build \
    ${MESON_OPTIONS/-D b_lto=true/} \
    --buildtype plain \
    -D systemd=true \
    -D systemd_session=enable \
    -D systemd_journal=true

ninja -C build
ninja -C build install

